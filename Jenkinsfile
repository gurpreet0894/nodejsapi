pipeline {
  agent any
  stages {
    stage('test') {
      steps {
        echo 'inside the test'
      }
    }
      stage('build') {
      steps {
        echo 'inside the build'
      }
    }
  }
}
